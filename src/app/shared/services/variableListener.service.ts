import { Subject } from 'rxjs/Subject';

export class VariableListenerService{
    // type: number
    customizedItemNavSelectedIdListenet = new Subject();
    // type: IIdName[]
    //selectedToppingArrListenet = new Subject();
}